{
  "buildCommand": "pnpm turbo run build --filter=@recipe4you/web...",
  "outputDirectory": "apps/web/dist",
  "installCommand": "corepack prepare pnpm@9.15.9 --activate && pnpm install --frozen-lockfile",
  "framework": "vite"
}
